EXPO_PUBLIC_API_URL=http://localhost:8000
EXPO_PUBLIC_ENV=development
# Dev only: pin to a seeded device ID so the app sees test data.
# Run scripts/seed-dev.sh first, then uncomment:
# EXPO_PUBLIC_DEV_DEVICE_ID=vital-dev-seed-001
